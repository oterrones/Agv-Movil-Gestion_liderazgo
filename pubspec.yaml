name: rrhh
description: Contratos Móviles
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # cupertino_icons: ^1.0.2
  provider: ^6.0.1
  intl: ^0.17.0
  nb_utils: ^4.4.14
  path_provider: ^2.0.9
  sqflite: ^2.0.2
  http: ^0.13.4
  flutter_easyloading: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  fonts:
    - family: Quicksand
      fonts:
        - asset: static/fonts/Quicksand-Regular.ttf

  assets:
    - static/images/
    - static/images/login/
